function Button({children}) {
    return (
        <button className="inline-block rounded-full bg-lightGreen px-[30px] py-[15px]">
            {children}
        </button>
    )
}

export default Button
